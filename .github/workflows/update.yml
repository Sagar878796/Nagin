name: IPTV Live TV Only Playlist

on:
  schedule:
    - cron: "0 */6 * * *"
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Download Source Playlist
        run: |
          curl -L "http://desyra.co:8080/get.php?username=gRVSxCJCuX&password=yummy9training&type=m3u_plus&output=ts" -o full.m3u

      - name: Extract Only Live TV (Remove Movies/Series)
        run: |
          echo "#EXTM3U" > playlist.m3u

          awk '
          BEGIN{IGNORECASE=1}

          /#EXTINF/ {
            info=$0
            getline url

            # Skip VOD categories
            if (info ~ /(movie|vod|series|film|cinema)/) next

            # Keep only real TV categories
            if (info ~ /(sports|news|entertainment|kids|music|hindi|india|tv)/) {
              print info
              print url
            }
          }
          ' full.m3u >> playlist.m3u

      - name: Remove Duplicate Channels
        run: |
          awk '!seen[$0]++' playlist.m3u > temp.m3u
          mv temp.m3u playlist.m3u

      - name: Commit & Push
        run: |
          git config --global user.name "bot"
          git config --global user.email "bot@users.noreply.github.com"
          git add playlist.m3u
          git commit -m "Auto update $(date)" || echo "No changes"
          git push
