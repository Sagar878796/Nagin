name: IPTV Ultimate Playlist (India + Proxy + Stable)

on:
  schedule:
    - cron: "0 */6 * * *"
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repo
        uses: actions/checkout@v4

      - name: Download Playlist
        run: |
          URL="http://desyra.co:8080/get.php?username=gRVSxCJCuX&password=yummy9training&type=m3u_plus&output=ts"

          for i in 1 2 3; do
            echo "Attempt $i..."
            curl -L -A "Mozilla/5.0" --connect-timeout 10 --max-time 30 "$URL" -o full.m3u && break
            sleep 5
          done

      - name: Validate Playlist
        run: |
          if [ ! -s full.m3u ]; then
            echo "Download failed"
            exit 1
          fi

          grep -q "#EXTM3U" full.m3u || exit 1

      - name: Filter + Apply Proxy
        run: |
          echo "#EXTM3U" > playlist.m3u

          PROXY="https://bitter-sunset-2179.sagarsutradhar046.workers.dev/?url="

          awk -v proxy="$PROXY" '
          /#EXTINF/ {
            info=$0
            getline url

            # Skip movies/series
            if (url ~ /\/movie\// || url ~ /\/series\//) next
            if (info ~ /(VOD|Movies|Series)/) next

            # Only Indian
            if (info !~ /(IN|India|Hindi|Tamil|Telugu|Punjabi|Bengali|Marathi|Malayalam|Kannada|Sports|Cricket|IPL)/) next

            print info
            print proxy url
          }
          ' full.m3u >> playlist.m3u

      - name: Remove Duplicates
        run: |
          awk '!seen[$0]++' playlist.m3u > tmp.m3u
          mv tmp.m3u playlist.m3u

      # ✅ FIXED VERSION (No crash)
      - name: Remove Dead Links (Stable)
        run: |
          echo "#EXTM3U" > final.m3u

          while IFS= read -r line; do
            if [[ "$line" == \#EXTINF* ]]; then
              read -r url || break

              code=$(curl -o /dev/null -s -w "%{http_code}" --max-time 8 "$url")

              if [[ "$code" == "200" || "$code" == "301" || "$code" == "302" ]]; then
                echo "$line" >> final.m3u
                echo "$url" >> final.m3u
              fi
            fi
          done < playlist.m3u

          mv final.m3u playlist.m3u

      - name: Final Safety Check
        run: |
          if [ ! -s playlist.m3u ]; then
            echo "Final playlist empty"
            exit 1
          fi

      - name: Commit & Push
        run: |
          git config --global user.name "iptv-bot"
          git config --global user.email "bot@users.noreply.github.com"

          git add playlist.m3u
          git diff --cached --quiet && echo "No changes" && exit 0

          git commit -m "🔥 Stable auto update $(date)"
          git push
